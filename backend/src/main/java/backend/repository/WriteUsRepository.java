package backend.repository;

import backend.models.WriteUs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WriteUsRepository extends JpaRepository<WriteUs, Integer> {
}
